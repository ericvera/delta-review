# Progress

## 01_01 — Pure status-filter and cluster body-state helpers in src/clusters.ts

- Key changes: `src/clusters.ts` — added exported `filterByStatus(files, status)` (order-preserving, reference-keeping filter) and `clusterBodyState(files)` returning the exported `ClusterBodyState` type (`"no-files" | "all-reviewed" | "has-needs-review"`); `src/clusters.test.ts` — new `describe` blocks for both helpers.
- Deviations from plan: named the filter helper `filterByStatus` (not `filesWithStatus`) per the task's own alternative, to avoid shadowing `ReviewTreeProvider`'s private `filesWithStatus`; Task 2.1 should use `filterByStatus`. Also exported the `ClusterBodyState` type alias alongside the function (matches the file's `ClusterContextValue` precedent).

## 02_01 — Grouped mode renders a bottom Reviewed bucket; clusters show only needs-review rows

- Key changes: `src/treeProvider.ts` — added `ReviewedBucketElement` (`kind: "reviewedBucket"`) to the element and collapsible unions; `FolderElement.inReviewedBucket?: true` third scope; deleted `FileElement.grouped` and the `✓` description suffix; `collapseKeyFor` handles the bucket (`"reviewedBucket"` namespace, collision-free with the ungrouped group's `"reviewed"`); grouped root pushes Unclustered/Auto only with a needs-review member and always appends the bucket last; `clusterChildren` filters bodies to needs-review via `filterByStatus` and switches on `clusterBodyState` (empty → existing message, all reviewed → `All files reviewed.`); new reviewedBucket branches in `getChildren` (all reviewed files, list = plain rows / tree = `treeChildren` with the bucket scope, relayed through the folder-recursion call site) and `getTreeItem` (label `Reviewed`, `check` icon, plain count over the whole model, id `reviewedBucket`, contextValue `reviewedGroup`); `folderScopeFiles` gained the bucket scope and cluster scope now returns needs-review only; cluster folders are always `needsReviewFolder` (removed the `hasNeedsReview` scan), bucket folders `reviewedFolder`; folder `item.id` now uses `collapseKeyFor(element)`.
- Deviations from plan: none. Known mid-phase state (per task): folder bulk `−` inside the bucket skips auto files until Task 2.2 fixes `folderScopeFiles` in `extension.ts`. Manual-pass substitute was executed as a scripted `@vscode/test-electron` extension-host suite (47 assertions: mocks 1A/2/3A, mark/unmark/edit round-trips via real commands, deep-folder scope relay, collapse-key isolation, ungrouped view unchanged) — harness in session scratchpad `vstest/` (`runBucket.mjs` + `suite/bucket.cjs`).

## 02_02 — Reviewed-bucket folder scope in extension.ts + DEVELOPMENT.md for grouped mode

- Key changes: `src/extension.ts` — `folderScopeFiles` gained a first `inReviewedBucket` branch returning `model?.files ?? []` (all triages; the commands' status+prefix filters narrow it), parameter widened to `{ clusterKey?: string; inReviewedBucket?: true }`, comment rewritten around visible-children semantics; `DEVELOPMENT.md` — ClusterModel flow gained grouped-rendering bullets (needs-review-only cluster bodies, Unclustered/Auto header hiding, always-present Reviewed bucket with `reviewedGroup` contextValue, `n/n` + `All files reviewed.` rows, full-membership counts, bucket folder `−` covering auto + `markAutomatically` bounce-back, mark/unmark ref write as what moves rows) and manual script steps 18-19 rewritten for the bucket (numbering 17-22 intact).
- Deviations from plan: none. Manual-pass substitute (config Test exception): extension-host suite `runScope.mjs` + `suite/scope.cjs` in session scratchpad `vstest/` (22 assertions, all passed) — real `unmarkFolderReviewed` with a bucket-scoped folder unmarks the auto child; non-bucket folder `−` still excludes auto; cluster folder branch untouched; `markAutomatically` self-mark and bounce-back after bucket folder `−`; `git ls-tree refs/review/feature` byte-identical across lever/view flips (REQ-FLOW-3); steps 21-22 — invalid contract → `invalid` + one-line error, cluster bulk no-op, ungrouped fallback with preference intact; deleted → `missing`, still no-op; restored → grouped returns and cluster bulk works again (REQ-PRESERVE-2).
