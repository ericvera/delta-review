# Progress

## 1.1 — Switched build/package to an esbuild bundle; added picomatch

- Key changes: new `esbuild.mjs` (bundles `src/extension.ts` → `out/extension.js`, cjs, node platform, `external: ['vscode']`, sourcemap, no minify, `--watch` flag); `package.json` scripts `build` → `tsc -p . && node esbuild.mjs`, `watch` → `node esbuild.mjs --watch`; deps `picomatch` + dev `esbuild`, `@types/picomatch`; `tsconfig.json` adds `noEmit: true` and `esModuleInterop: true`; `.vscodeignore` now also excludes `esbuild.mjs`, `eslint.config.mjs`, `.prettierignore`, `.claude/**`, `.mise/**`; `README.md` dev link de-linkified.
- Deviations from plan:
  - Added `esModuleInterop: true` to tsconfig (not in plan): `@types/picomatch` uses `export =`, so the default-import form (`import picomatch from "picomatch"`, needed by task 1.2) fails typecheck without it; esbuild handles the interop natively.
  - Fixed `README.md`: `yarn package` failed at baseline too — vsce errors on the relative `[DEVELOPMENT.md](DEVELOPMENT.md)` link with no repository URL. Replaced with plain-text mention so packaging passes.
  - `.vscode/tasks.json` unchanged: its build task runs `yarn build` (not tsc directly), which still typechecks + bundles as a one-shot preLaunchTask, so F5 keeps working.
- Verification: format/lint/build/test green; `yarn package` produces a vsix containing only `out/extension.js(.map)` + docs; picomatch confirmed inlined in the bundle (no bare `require("picomatch")`); headless extension-host smoke test (`@vscode/test-electron`, scratchpad harness) activated the bundled extension against a git repo with changes, all `deltaReview.*` commands registered, `deltaReview.refresh` executed cleanly (substitute for manual F5 per test exception).
- Review fix: added `PLAN.md` to `.vscodeignore` — the internal implementation brief was shipping inside the vsix. Re-verified: format/lint/build/test green; `yarn package` vsix file list no longer contains `extension/PLAN.md` (only `out/extension.js(.map)`, `package.json`, `readme.md`, `DEVELOPMENT.md`).

## 1.2 — Triage classification (`auto`/`normal`) on ReviewFile

- Key changes: new `src/triage.ts` (`Triage` type + `computeTriage(paths, globs, generatedPaths)`, picomatch with `dot: true`, matchers precompiled per call); new `src/triage.test.ts` (10 Vitest cases: glob hit, `**` recursion, generated-only, both-empty, combination, dotfiles, case sensitivity, deleted path, pattern order, full coverage of input paths); `src/model.ts` adds `ReviewFile.triage`, `fetchGeneratedPaths` (`git check-attr --stdin -z linguist-generated`, NUL-joined stdin, triplet walk, try/catch → empty set) and an `options?: { autoReviewGlobs?: string[] }` parameter on `computeReviewModel`; `src/extension.ts` `refresh()` reads `autoReview.globs` and passes it through; `package.json` declares `deltaReview.autoReview.globs` (array, default `[]`) and `deltaReview.autoReview.markAutomatically` (boolean, default `false`).
- Deviations from plan: none.
- Verification: format/lint/build/test green. Integration smoke (bundled `computeReviewModel` against a temp repo with `.gitattributes` `gen/* linguist-generated`): generated paths (tracked and untracked) and `**/*.lock` matches → `auto`, others (incl. a path with spaces/`$`) → `normal`. Headless extension-host smoke (`@vscode/test-electron` per memory recipe, substitute for F5 per test exception): extension activated with workspace `autoReview.globs` set, both new settings resolved (workspace value + `false` default), `deltaReview.refresh` + `markAllReviewed` produced `refs/review/feature` containing the full review set — no extension-host errors from the extension (pre-existing warning about `deltaReview.openDiff` missing from `contributes.commands` is untouched baseline).
- Review fix: two confirmed defects fixed. (1) `git check-attr -z` triplet misalignment on empty attribute values (`linguist-generated=` emits `path\0attr\0\0`; `splitNulTerminated` dropped the empty field, shifting every later triplet) — extracted a pure `parseCheckAttrOutput` in `src/model.ts` that splits on `\0` without filtering empties (dropping only the trailing terminator field) and added `src/model.test.ts` covering set/true/unspecified, the empty-value alignment case, and empty output. (2) Invalid globs in `deltaReview.autoReview.globs` blanked the whole panel (`picomatch("")` throws inside `computeReviewModel`) — `computeTriage` now takes `readonly unknown[]`, skips non-string/empty entries, and wraps per-glob compilation in try/catch; added tests for empty-string glob, non-string entries, and an over-length throwing pattern. Re-verified: format/lint/build/test green (16 tests); integration run of bundled `computeReviewModel` against a temp repo with a `linguist-generated=` entry plus an empty glob → path after the empty-value entry correctly `auto`, no crash.

## 2.1 — Auto subgroups + bulk mark/unmark in list and tree modes

- Key changes: `src/treeProvider.ts` adds `AutoGroupElement {kind:'autoGroup', status}` to `ReviewTreeElement`; `getChildren` on a group prepends the Auto subgroup when auto files exist and feeds only `normal` files to the list/tree paths (`treeChildren` now iterates `filesWithStatus(status, "normal")`); autoGroup children are flat `FileElement`s with a new optional `inAutoGroup: true` flag that forces the directory description in tree mode too; `getTreeItem` renders autoGroup (label `Auto`, `ThemeIcon("gear")`, count description, `id`/collapse key `autoGroup:<status>`, contextValue `needsReviewAutoGroup`/`reviewedAutoGroup`, tooltip); `filesWithStatus` gained an optional `Triage` filter; `collapseKeyFor` widened to autoGroup. `src/extension.ts`: provider callback widened to `isCollapsed(key, defaultCollapsed)` (default-collapsed elements are collapsed unless `expanded:<key>` is in the persisted set); collapse/expand listeners store/remove `expanded:<key>` for autoGroup (inverse convention, group/folder behavior unchanged); new commands `deltaReview.markAutoReviewed` / `deltaReview.unmarkAutoReviewed` (guarded like markFolderReviewed, filter `triage === 'auto'` + status, bulk mark/unmark, refresh). `package.json`: both commands declared (`$(add)`/`$(remove)`), inline+navigation `view/item/context` entries on the new contextValues, hidden from the palette.
- Deviations from plan: none. Skipped the optional `src/treeProvider.test.ts` (no pure extraction made sense — provider logic is vscode-bound; Task 3.1's pure-core tests + the headless harness below cover it).
- Verification: format/lint/build/test green (16 tests). E2E substitute (test exception, headless `@vscode/test-electron` harness per memory recipe): (a) provider structure asserted in-host against a bundled `treeProvider` with a fake model — auto subgroup first under both status groups in both layouts, flat alphabetical contents with dir descriptions, gear/count/contextValues, collapsed-by-default honoring `expanded:<key>`, `gen/` auto-only folder absent from the tree, group counts still include auto files, bare-key group collapse convention unchanged, and a no-auto model produces the exact legacy structure; (b) live fixture repo (glob `**/*.lock` + `gen/* linguist-generated`) — `markAutoReviewed` put exactly `gen/out.js` + `yarn.lock` in `refs/review/feature`, wrong-kind element was a no-op, `unmarkAutoReviewed` emptied the state, `markAllReviewed` still covered all four changed files.
- Review fix: folder bulk actions acted on auto files that no longer render under the folder — `deltaReview.markFolderReviewed`/`unmarkFolderReviewed` filtered by status + path prefix only, so a folder's ✓ marked auto-triaged files (e.g. `src/deps.lock`) that render only inside the Auto subgroup. Added `triage === "normal"` to both filters in `src/extension.ts` so folder actions cover only the folder's visible children; `markAllReviewed`/`unmarkAllReviewed` untouched (intentionally cover everything). Re-verified: format/lint/build/test green (16 tests); headless harness extended with `src/deps.lock` (auto via glob) alongside `src/app.ts` — folder ✓ on `src` marked only `src/app.ts`, `markAutoReviewed` still covered `src/deps.lock`, folder − on `src` unmarked only `src/app.ts` leaving the auto file reviewed.

## 2.2 — `deltaReview.autoReview.markAutomatically`: auto files self-mark during refresh

- Key changes: `src/extension.ts` `refresh()` only — after `computeReviewModel` succeeds and the generation check passes, if `autoReview.markAutomatically` is true it collects `triage === "auto" && status === NeedsReview` paths, calls `markReviewed` (normal snapshot path), recomputes the model once, and re-checks the generation counter after each await; all of it before `treeProvider.setModel` so no needs-review flash. No suppression state added (ref-write-triggered refreshes find nothing to mark).
- Deviations from plan: none.
- Verification: format/lint/build/test green (16 tests). E2E substitute (test exception, headless `@vscode/test-electron` harness per memory recipe; fixture: glob `**/*.lock`, `gen/* linguist-generated`, plus `old.lock` deleted on the branch): default false → refreshes mark nothing; workspace-config flip to true (no reload) → next refresh put exactly `gen/out.js` + `yarn.lock` + `old.lock` in `refs/review/feature` with working-content blob shas (`old.lock` = deletion sentinel), normal `src/app.ts` untouched, `git log` on the ref shows normal `delta-review state` commits; editing `yarn.lock` made it a delta and the next refresh re-marked it to the new sha; flip back to false + edit → stale mark persists (needs-review by content comparison), repeated refreshes never re-mark.

## 3.1 — Clusters contract: locate, parse, validate, resolve membership (pure core)

- Key changes: new `src/clusters.ts` (vscode-free) — types `ClusterDefinition`/`ClustersContract`/`ClusterBucket`/`ClusterModel`, `sanitizeBranchForFilename` (chars outside `[A-Za-z0-9._-]` → `-`), `parseClustersContract(text)` → `{ok, contract}|{ok:false, error}` (version must be integer 1, clusters array, each entry string label/summary + ≥1 non-empty string array among files/patterns; absent files/patterns normalize to `[]`; unknown keys ignored; one-line user-facing errors), `resolveClusterModel(contract, files)` → `{clusters, unclustered, auto}` (auto triage wins over everything; explicit files beat patterns; first-listing/first-matching cluster wins; picomatch `dot:true` with per-pattern try/catch like triage.ts; input order preserved; ReviewFile kept by reference), `loadClustersContract(git, branch)` → `missing|invalid|ok` reading `<common dir>/delta-review/clusters-<sanitized>.json` (`rev-parse --git-common-dir` resolved against `git.repoRoot` when relative; ENOENT → missing, other read errors → invalid). New `src/clusters.test.ts` (41 Vitest cases) covering sanitization, version/shape rejection, all membership rules, order/reference preservation, and the loader against temp dirs with a fake `Git` (relative + absolute common dir, missing/ok/invalid/EISDIR).
- Deviations from plan: none. (Loader also covered by unit tests with a fake `Git`, beyond the planned manual check.)
- Verification: format/lint/build/test green (57 tests). Manual loader check (scratchpad script bundled with esbuild against a real temp git repo): missing → `{state:"missing"}`, hand-written valid contract at `.git/delta-review/clusters-feature-x.json` → `ok` with parsed clusters, version 3 → `invalid` "unsupported version 3 (extension supports 1)", malformed JSON → `invalid`, and a linked worktree (absolute `--git-common-dir`) still finds the main repo's contract. E2E: test exception (no e2e infra) — unit tests are the verification; UI lands in Tasks 3.2/3.3.

## 3.2 — Grouping lever, context keys, contract watching, invalid-contract warning

- Key changes: `src/extension.ts` — `clusterModel: ClusterModel | undefined` next to `model`; workspaceState key `deltaReview.grouped` (default false) + context keys `deltaReview.grouped`/`deltaReview.clustersAvailable` (set on activation and every change; `setClustersAvailable` helper); `setGrouped` mirroring `setViewMode`; commands `deltaReview.groupByCluster`/`ungroupClusters`; `refresh()` loads the contract via `loadClustersContract(git, computed.branch)` after auto-marking (with a generation re-check after the await) — `ok` → `resolveClusterModel`, `missing` → cleared, `invalid` → cleared + `treeView.message = "⚠ Clusters contract: <error>"`; fatal model errors (catch branch) still win and reset cluster state; new `watchContractDir(gitInstance)` resolves `rev-parse --git-common-dir` (absolute-or-relative) and watches `<common dir>/delta-review/*.json` → `scheduleRefresh`, disposables appended to `repoWatcherDisposables`, stale resolution discarded when the active repo changed mid-await; wired in `setActiveRepo`. `package.json` — commands `groupByCluster` (`$(group-by-ref-type)`) / `ungroupClusters` (`$(ungroup-by-ref-type)`), `view/title` entries at `navigation@1` gated on `deltaReview.clustersAvailable` + `!deltaReview.grouped`/`deltaReview.grouped`, refresh shifted to `navigation@2`; palette-visible like viewAsTree/viewAsList. Grouped rendering itself is Task 3.3; the stored preference is never erased by contract loss (effective grouping = preference && clusterModel present).
- Deviations from plan: warning text is `⚠ Clusters contract: <error>` rather than bare `⚠ <error>` — the parse errors ("unsupported version 3 …") need the subject to be intelligible in the view message slot.
- Verification: format/lint/build/test green (57 tests). E2E substitute (test exception, headless `@vscode/test-electron` harness per memory recipe; new variant: bundle copied into a stub test extension that contributes the `deltaReview` view, so the suite spies on `createTreeView`/`executeCommand(setContext)` and a fake `ExtensionContext` memento — real delta-review extension not loaded): fixture repo on branch `feature/x` → (1) no contract: no warning, both context keys false, both commands registered; (2) creating `.git/delta-review/clusters-feature-x.json` picked up with no manual refresh → `clustersAvailable` true; (3) lever commands toggle the `deltaReview.grouped` context and persist to workspaceState; (4) overwrite with `{"version":3}` → `clustersAvailable` false, `treeView.message` = "⚠ Clusters contract: unsupported version 3 …", stored preference intact; (5) delete → warning clears; (6) recreate valid → available again, grouping preference survived; (7) fresh-activation read of the persisted key returns the surviving preference.

## 3.3 — Clustered rendering: clusters → Unclustered → Auto, bulk actions, collapse persistence

- Key changes: `src/treeProvider.ts` — new element kinds `ClusterElement {kind:'cluster', clusterKey}` (`c<index>` | `unclustered` | `auto`; files re-resolved from `getClusterModel()` at render time, never snapshotted) and `MessageElement`; ctor gains `getClusterModel()` + `isGrouped()` callbacks; grouped root renders clusters in contract order (empty ones included) then Unclustered/Auto when non-empty, ungrouped root is the untouched legacy path; `treeChildren(files, parentPath, scope)` parameterized by file source + scope (`status` xor `clusterKey`), `FolderElement.status` now optional with optional `clusterKey`; cluster children: Unclustered/Auto always flat, real clusters follow the layout lever, empty cluster → message row; `FileElement.inAutoGroup` renamed `alwaysFlat` (same mechanism, now also covers grouped Auto/Unclustered rows) plus new `grouped` flag → reviewed rows append ` ✓` to the description (`dir ✓` flat, bare `✓` tree-nested); cluster items: layers/warning/gear ThemeIcons, `n/m` counts (Auto plain until first reviewed), summary/explanatory tooltips, `id`/collapse key `cluster:<key>`, contextValues `clusterNeedsReview|clusterReviewed|clusterEmpty`; cluster-scoped folders: id/collapse key `folder:<clusterKey>:<path>`, contextValue from the folder's cluster files (mixed statuses); exported `isDefaultCollapsed` (autoGroup + Auto cluster) drives the inverted `expanded:<key>` persistence in both listeners. `src/clusters.ts` — pure helpers `clusterBucketForKey`, `clusterFilesForKey`, `clusterContextValue`, `clusterCountDescription`. `src/decorations.ts` — `createUnclusteredHeaderUri()` with query `unclustered-header` → badge-less `list.warningForeground` decoration (warning-colored Unclustered label; children unaffected). `src/extension.ts` — provider wired with cluster model + effective-grouped callbacks; collapse listeners generalized via `isDefaultCollapsed` and skip `message`; new commands `deltaReview.markClusterReviewed`/`unmarkClusterReviewed` (resolve files via `clusterFilesForKey`, filter status, bulk mark/unmark); folder commands now use `folderScopeFiles` (cluster's file list when `element.clusterKey` set, else normal-triage model files). `package.json` — both commands declared (`$(add)`/`$(remove)`), inline@2+navigation menu entries on the cluster contextValues, hidden from palette. `src/clusters.test.ts` — 10 new cases for the four helpers (67 total).
- Deviations from plan: `inAutoGroup` renamed to `alwaysFlat` since the same flag now marks grouped Unclustered/Auto rows (plan said "reuse that mechanism"; the old name would have been wrong for Unclustered). No dimmed decoration variant added (per plan's Gotchas default). Unclustered description is plain `n/m` and Auto plain count per task spec (mock v2's extra description suffixes intentionally not replicated).
- Verification: format/lint/build/test green (67 tests). E2E substitute (test exception, headless `@vscode/test-electron` harness per memory recipe, real extension loaded against a fixture repo with a 2-cluster contract — one empty — plus unclustered and auto files): (a) structure suite (bundled treeProvider, fake models): grouped root order c0→c1→unclustered→auto; ungrouped root/children byte-identical to legacy; cluster items' labels/counts/tooltips/icons/contextValues incl. `0/0` + `clusterEmpty` + message row for the empty cluster; Unclustered `warning` icon + `unclustered-header` resourceUri query; Auto plain count → `1/2` → `2/2` and collapsed-by-default honoring `expanded:cluster:auto`; flat rows `dir ✓` and tree-nested rows bare `✓` for reviewed files; Unclustered/Auto rows keep dir in tree mode; cluster-scoped folder id/key `folder:c0:src` with mixed-status contextValue flip, legacy folder keys unchanged; vanished cluster model degrades to ungrouped without throwing. (b) live suite: lever flips (both levers, both directions) leave `refs/review/feature` byte-identical; `markClusterReviewed` c0 marked exactly the cluster's 2 files, empty-cluster and wrong-kind invocations no-ops; unclustered/auto bulk marks and cluster-scoped unmark each hit exactly their bucket; cluster-scoped folder ✓ on `src` marked only c0's files (not unclustered `src/misc.ts`); ungrouped folder ✓ still covers all normal files under `src` excluding auto.

## 4.1 — Claude Code plugin: marketplace + plugin manifests, cluster-review skill

- Key changes: new `.claude-plugin/marketplace.json` (marketplace `delta-review`, owner, one plugin entry `cluster-review` with `source: "./plugin"`); new `plugin/.claude-plugin/plugin.json` (`cluster-review` 0.1.0); new `plugin/skills/cluster-review/SKILL.md` (frontmatter name/description with triggers; body: resolve base branch from `.vscode/settings.json` `deltaReview.baseBranch` else `main` with the merged-config divergence documented, review set via `git merge-base` + `git diff --name-only --no-renames -z` + `git ls-files --others --exclude-standard -z` deduplicated, contract path `<git common dir>/delta-review/clusters-<sanitized>.json` with the `[^A-Za-z0-9._-]` → `-` rule stated verbatim and the relative-common-dir caveat, incremental re-run keeping labels stable, clustering quality bar (narrative not directories, patterns only for glob catch-alls, no junk-drawer cluster), atomic temp-file+rename write, never commit/push/stage, schema stated inline with the ≥1-of-files/patterns rule and the version-bump-with-`src/clusters.ts` note); `.vscodeignore` adds `.claude-plugin/**` and `plugin/**`.
- Deviations from plan: none.
- Verification: format/lint/build/test green (67 tests). Both manifests parse; field names verified against installed plugin caches (`~/.claude/plugins/cache/ericvera/mise`, official marketplace) and `claude --plugin-dir ./plugin plugin details cluster-review` loads the plugin and lists the skill. Hand-executed SKILL.md on this repo/branch (`agentic-change-review`, base `main`, 37-file review set) → contract at `.git/delta-review/clusters-agentic-change-review.json`; real `parseClustersContract` (bundled `src/clusters.ts`) accepts it and `resolveClusterModel` claims all 37 files (0 unclustered). Headless extension-host run (test exception substitute for F5, memory recipe) with the real extension opened on this repo: `groupByCluster` executed, `markClusterReviewed` on `c3` marked exactly the 3 plugin files in `refs/review/agentic-change-review`, empty-bucket no-op, unmark emptied state, ref cleaned up. Incremental re-run simulated (new untracked file → labels stable, file assigned to its cluster, 38/38 claimed; removal re-derived 37/37). `yarn package` vsix contains no `plugin/` or `.claude-plugin/` entries.
- Review fix: two accuracy defects in `plugin/skills/cluster-review/SKILL.md`. (1) The schema section claimed the extension "validates" rules it does not reject — split the list into rules `parseClustersContract` actually rejects (version integer 1, clusters array, object shape, string label/summary, string-array files/patterns, ≥1 non-empty of files/patterns) vs. conventions the extension fails silently on (bad path format → never matches → Unclustered; uncompilable picomatch globs silently ignored; SHAs/unknown keys ignored, not rejected), each convention now stating its silent failure mode. (2) Step 1 read `deltaReview.baseBranch` from `.vscode/settings.json` without noting VS Code settings are JSONC — added a clause to read as text and tolerate comments/trailing commas rather than assuming strict `JSON.parse`/`jq`. Re-verified: rejected-rules list checked line-by-line against `src/clusters.ts`; format/lint/build/test green (67 tests).

## 5.1 — Docs for both features + full acceptance sweep (headless substitute for the F5 pass)

- Key changes: `README.md` (feature bullets for auto-review + clustered review, "Auto-review" and "Cluster with Claude Code" sections with the two `/plugin` install commands and a one-line contract explanation, new "Settings" section listing all three settings; still a lean overview); `DEVELOPMENT.md` (esbuild bundle description, "Auto triage" inputs, "Clusters contract" path/sanitization/schema + "ClusterModel flow", manual test script extended to 17 steps covering the sweep scenarios). No source changes — the sweep caught no defects.
- Deviations from plan: the "F5 dev host" sweep ran headlessly (cannot drive a GUI) via a scripted `@vscode/test-electron` harness (scratchpad `vstest/sweep/`, memory recipe), phase by phase:
  - **REQ-PRESERVE-2 baseline**: `git archive main` built into a second stub extension; an identical 14-step probe (fixture: no contract, default settings, no linguist-generated; counts, badges, status bar, M/A/D decorations, diff calls + left-side contents via the content provider, mark/unmark, folder actions, collapse persistence, mark-all, clearReviewState modal, porcelain) serialized 75 KB snapshots from the current and main builds — deep-equal, byte-identical (method: baseline build of `main`, not an installed vsix).
  - **Features probe** (25 checks, in-host activation of the real bundle with spied vscode facade): linguist-generated with empty globs; globs applied live without reload; Auto subgroup structure in both layouts incl. `expanded:` collapse convention; folder actions exclude auto; bulk ✓/− on Auto; Mark All covers everything; markAutomatically self-mark via snapshot path (blob sha == `git hash-object`), re-mark on edit, resurface when off; contract create/change/delete watched live; grouped rendering (contract order, `n/m` non-auto counts, summary tooltips, dimmed-✓ reviewed rows, warning-styled Unclustered omitted when empty, Auto last/collapsed/plain-count-until-reviewed, empty-cluster message row); tree-in-clusters with scoped folder ids/collapse keys/actions; cluster/Unclustered/Auto bulk actions exact; diff titles unchanged; lever flips leave `refs/review/<branch>` byte-identical; invalid contract ⚠ + fallback + preference survives; zero footprint (porcelain identical, contract under `.git`).
  - **Live probe** (real extension as dev extension): activation, contract consumption, markClusterReviewed exact, markAllReviewed, SCM repo switching via `workbench.scm.action.toggleRepositoryVisibility.scm<N>` (registered after opening the SCM viewlet) retargeting state per repo/branch, porcelain clean in both repos.
  - **Static**: `package.json` menu `when` wiring (grouping button icon-swap gated on `clustersAvailable`/`grouped`, auto/cluster bulk actions on their contextValues, bulk commands palette-hidden).
  - Needs human eyes in a real dev host (visual-only, data verified programmatically): pixel rendering of icons/colors (gear, layers, warning tint, dimmed reviewed rows), hover placement of inline +/− actions, and the feel of the icon-swap on the view title.
- Verification: format/lint/build/test green (67 tests); `yarn package` vsix contains only bundle + docs (no plugin/, .claude-plugin/, .mise/, PLAN.md).
